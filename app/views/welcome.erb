<div class="container">
  <div>
    <% if logged_in? %>
      <br>
      <br>
      <h2>Welcome, <%=current_user.username %></h2>
    <% else %>
      <p>Welcome</p>
      <p>Please Log In or Sign Up below:</p>
      <a href="/signup" class="button button-1"><button type="button" class="button-1">Sign Up</button></a>
      <br>
      <br>
      <a href="/login"> Log In </a>
    <%end %>
  </div>
</div>
